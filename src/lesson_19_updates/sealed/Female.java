package lesson_19_updates.sealed;

// If inherited from sealed
// Class -> final (чтобы дальше не наследоваться), sealed (тоже задать ограничения), non-sealed (от меня можно
// наследоваться свободно)
public non-sealed class Female extends Person{
}
